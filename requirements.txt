# Core
pydantic>=2.6
numpy>=1.26
pandas>=2.1
python-dotenv>=1.0

# Retrieval (embeddings). sentence-transformers is optional at runtime — the
# embedder falls back to a deterministic hash embedding if it isn't installed —
# but installing it gives real semantic retrieval quality.
sentence-transformers>=2.7

# Vector store. FAISS is optional; an exact NumPy cosine search is used if absent.
faiss-cpu>=1.8 ; platform_system != "Darwin" or platform_machine != "arm64"

# Generation (choose a provider; all optional — mock mode needs none).
# The `openai` SDK also drives Groq (OpenAI-compatible) via a base_url override.
anthropic>=0.39
openai>=1.40

# API
fastapi>=0.110
uvicorn[standard]>=0.29

# UI
streamlit>=1.33

# Tests
pytest>=8.0
httpx>=0.27
